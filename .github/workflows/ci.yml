name: CI

on:
  workflow_call:
  workflow_dispatch:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
  push:
    branches:
      - main
  merge_group:

concurrency:
  group: ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.event.merge_group.head_ref || github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

permissions:
  contents: read

env:
  BETTER_AUTH_URL: http://localhost:3000

jobs:
  lint-workspace:
    name: Lint workspace
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Set up Ruby
        uses: ruby/setup-ruby@95ef2b042f9d7a56d8268cba8559e2842e2ad01b # v1.321.0
        with:
          ruby-version: .ruby-version
          bundler-cache: true

      - name: Run linter
        run: bundle exec standardrb

  test-workspace:
    name: Test workspace release metadata
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Install database client headers
        run: |
          set -euo pipefail
          for attempt in 1 2 3; do
            sudo apt-get update -o Acquire::Retries=3 -o Acquire::http::Timeout=10 -o Acquire::https::Timeout=10 && break
            if [ "$attempt" = "3" ]; then
              exit 1
            fi
            sleep 10
          done
          sudo apt-get install -y --no-install-recommends default-libmysqlclient-dev freetds-dev

      - name: Set up Ruby
        uses: ruby/setup-ruby@95ef2b042f9d7a56d8268cba8559e2842e2ad01b # v1.321.0
        with:
          ruby-version: .ruby-version
          bundler-cache: true

      - name: Run workspace packaging tests
        env:
          RUBYOPT: -r${{ github.workspace }}/test/support/strict_minitest
        run: bundle exec rake test:workspace

  test-core:
    name: Test better_auth (Core)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Fetch upstream Better Auth sources
        run: ./scripts/fetch-upstream-better-auth.sh

      - name: Install FreeTDS headers
        run: |
          if [ -f /etc/apt/apt-mirrors.txt ]; then
            echo "https://archive.ubuntu.com/ubuntu/" | sudo tee /etc/apt/apt-mirrors.txt
          fi
          sudo apt-get update -o Acquire::Retries=3 -o Acquire::http::Timeout=10 -o Acquire::https::Timeout=10
          sudo apt-get install -y --no-install-recommends freetds-dev

      - name: Set up Ruby
        uses: ruby/setup-ruby@95ef2b042f9d7a56d8268cba8559e2842e2ad01b # v1.321.0
        with:
          ruby-version: .ruby-version
          bundler-cache: true
          working-directory: packages/better_auth


      - name: Run tests
        working-directory: packages/better_auth
        env:
          RUBYOPT: -r${{ github.workspace }}/test/support/strict_minitest
        run: bundle exec rake test

  test-passkey:
    name: Test better_auth-passkey (Passkey Plugin)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Fetch upstream Better Auth sources
        run: ./scripts/fetch-upstream-better-auth.sh

      - name: Install FreeTDS headers
        run: |
          if [ -f /etc/apt/apt-mirrors.txt ]; then
            echo "https://archive.ubuntu.com/ubuntu/" | sudo tee /etc/apt/apt-mirrors.txt
          fi
          sudo apt-get update -o Acquire::Retries=3 -o Acquire::http::Timeout=10 -o Acquire::https::Timeout=10
          sudo apt-get install -y --no-install-recommends freetds-dev

      - name: Set up Ruby
        uses: ruby/setup-ruby@95ef2b042f9d7a56d8268cba8559e2842e2ad01b # v1.321.0
        with:
          ruby-version: .ruby-version
          bundler-cache: true
          working-directory: packages/better_auth-passkey


      - name: Run tests
        working-directory: packages/better_auth-passkey
        env:
          RUBYOPT: -r${{ github.workspace }}/test/support/strict_minitest
        run: bundle exec rake test

  test-cli:
    name: Test better_auth-cli
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Fetch upstream Better Auth sources
        run: ./scripts/fetch-upstream-better-auth.sh

      - name: Set up Ruby
        uses: ruby/setup-ruby@95ef2b042f9d7a56d8268cba8559e2842e2ad01b # v1.321.0
        with:
          ruby-version: .ruby-version
          bundler-cache: true
          working-directory: packages/better_auth-cli


      - name: Run tests
        working-directory: packages/better_auth-cli
        env:
          RUBYOPT: -r${{ github.workspace }}/test/support/strict_minitest
        run: timeout 180s bundle exec rake test

  test-api-key:
    name: Test better_auth-api-key (API Key Plugin)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Fetch upstream Better Auth sources
        run: ./scripts/fetch-upstream-better-auth.sh

      - name: Install FreeTDS headers
        run: |
          if [ -f /etc/apt/apt-mirrors.txt ]; then
            echo "https://archive.ubuntu.com/ubuntu/" | sudo tee /etc/apt/apt-mirrors.txt
          fi
          sudo apt-get update -o Acquire::Retries=3 -o Acquire::http::Timeout=10 -o Acquire::https::Timeout=10
          sudo apt-get install -y --no-install-recommends freetds-dev

      - name: Set up Ruby
        uses: ruby/setup-ruby@95ef2b042f9d7a56d8268cba8559e2842e2ad01b # v1.321.0
        with:
          ruby-version: .ruby-version
          bundler-cache: true
          working-directory: packages/better_auth-api-key


      - name: Run tests
        working-directory: packages/better_auth-api-key
        env:
          RUBYOPT: -r${{ github.workspace }}/test/support/strict_minitest
        run: bundle exec rake test

  test-rails:
    name: Test better_auth-rails (Rails Adapter)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Set up Ruby
        uses: ruby/setup-ruby@95ef2b042f9d7a56d8268cba8559e2842e2ad01b # v1.321.0
        with:
          ruby-version: .ruby-version
          bundler-cache: true
          working-directory: packages/better_auth-rails


      - name: Run fast tests
        working-directory: packages/better_auth-rails
        env:
          SPEC_OPTS: --require ${{ github.workspace }}/test/support/strict_rspec
        run: bundle exec rake test

  test-sinatra:
    name: Test better_auth-sinatra (Sinatra Adapter)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Set up Ruby
        uses: ruby/setup-ruby@95ef2b042f9d7a56d8268cba8559e2842e2ad01b # v1.321.0
        with:
          ruby-version: .ruby-version
          bundler-cache: true
          working-directory: packages/better_auth-sinatra


      - name: Run tests
        working-directory: packages/better_auth-sinatra
        env:
          SPEC_OPTS: --require ${{ github.workspace }}/test/support/strict_rspec
        run: bundle exec rspec

  test-roda:
    name: Test better_auth-roda (Roda Adapter)
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Set up Ruby
        uses: ruby/setup-ruby@95ef2b042f9d7a56d8268cba8559e2842e2ad01b # v1.321.0
        with:
          ruby-version: .ruby-version
          bundler-cache: true
          working-directory: packages/better_auth-roda


      - name: Run tests
        working-directory: packages/better_auth-roda
        env:
          SPEC_OPTS: --require ${{ github.workspace }}/test/support/strict_rspec
        run: timeout 180s bundle exec rspec

  test-hanami:
    name: Test better_auth-hanami (Hanami Adapter)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Set up Ruby
        uses: ruby/setup-ruby@95ef2b042f9d7a56d8268cba8559e2842e2ad01b # v1.321.0
        with:
          ruby-version: .ruby-version
          bundler-cache: true
          working-directory: packages/better_auth-hanami


      - name: Run fast tests
        working-directory: packages/better_auth-hanami
        env:
          SPEC_OPTS: --require ${{ github.workspace }}/test/support/strict_rspec
        run: bundle exec rake test

  test-grape:
    name: Test better_auth-grape (Grape Adapter)
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Set up Ruby
        uses: ruby/setup-ruby@95ef2b042f9d7a56d8268cba8559e2842e2ad01b # v1.321.0
        with:
          ruby-version: .ruby-version
          bundler-cache: true
          working-directory: packages/better_auth-grape


      - name: Run tests
        working-directory: packages/better_auth-grape
        env:
          SPEC_OPTS: --require ${{ github.workspace }}/test/support/strict_rspec
        run: timeout 180s bundle exec rspec

  test-oauth-provider:
    name: Test better_auth-oauth-provider (OAuth Provider Plugin)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Fetch upstream Better Auth sources
        run: ./scripts/fetch-upstream-better-auth.sh

      - name: Set up Ruby
        uses: ruby/setup-ruby@95ef2b042f9d7a56d8268cba8559e2842e2ad01b # v1.321.0
        with:
          ruby-version: .ruby-version
          bundler-cache: true
          working-directory: packages/better_auth-oauth-provider


      - name: Run tests
        working-directory: packages/better_auth-oauth-provider
        env:
          RUBYOPT: -r${{ github.workspace }}/test/support/strict_minitest
        run: bundle exec rake test

  test-redis-storage:
    name: Test better_auth-redis-storage (Redis Storage Plugin)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Fetch upstream Better Auth sources
        run: ./scripts/fetch-upstream-better-auth.sh

      - name: Set up Ruby
        uses: ruby/setup-ruby@95ef2b042f9d7a56d8268cba8559e2842e2ad01b # v1.321.0
        with:
          ruby-version: .ruby-version
          bundler-cache: true
          working-directory: packages/better_auth-redis-storage


      - name: Run fast tests
        working-directory: packages/better_auth-redis-storage
        env:
          RUBYOPT: -r${{ github.workspace }}/test/support/strict_minitest
        run: bundle exec rake test

  test-mongodb:
    name: Test better_auth-mongodb (MongoDB Adapter)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Set up Ruby
        uses: ruby/setup-ruby@95ef2b042f9d7a56d8268cba8559e2842e2ad01b # v1.321.0
        with:
          ruby-version: .ruby-version
          bundler-cache: true
          working-directory: packages/better_auth-mongodb


      - name: Run tests
        working-directory: packages/better_auth-mongodb
        env:
          RUBYOPT: -r${{ github.workspace }}/test/support/strict_minitest
        run: bundle exec rake test

  test-mongo-adapter:
    name: Test better_auth-mongo-adapter (MongoDB compatibility package)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Set up Ruby
        uses: ruby/setup-ruby@95ef2b042f9d7a56d8268cba8559e2842e2ad01b # v1.321.0
        with:
          ruby-version: .ruby-version
          bundler-cache: true
          working-directory: packages/better_auth-mongo-adapter


      - name: Run tests
        working-directory: packages/better_auth-mongo-adapter
        env:
          RUBYOPT: -r${{ github.workspace }}/test/support/strict_minitest
        run: bundle exec rake test

  test-scim:
    name: Test better_auth-scim (SCIM Plugin)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Fetch upstream Better Auth sources
        run: ./scripts/fetch-upstream-better-auth.sh

      - name: Set up Ruby
        uses: ruby/setup-ruby@95ef2b042f9d7a56d8268cba8559e2842e2ad01b # v1.321.0
        with:
          ruby-version: .ruby-version
          bundler-cache: true
          working-directory: packages/better_auth-scim


      - name: Run tests
        working-directory: packages/better_auth-scim
        env:
          RUBYOPT: -r${{ github.workspace }}/test/support/strict_minitest
        run: bundle exec rake test

  test-oidc:
    name: Test better_auth-oidc (OIDC SSO)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Set up Ruby
        uses: ruby/setup-ruby@95ef2b042f9d7a56d8268cba8559e2842e2ad01b # v1.321.0
        with:
          ruby-version: .ruby-version
          bundler-cache: true
          working-directory: packages/better_auth-oidc


      - name: Run tests
        working-directory: packages/better_auth-oidc
        env:
          RUBYOPT: -r${{ github.workspace }}/test/support/strict_minitest
        run: bundle exec rake test

  test-saml:
    name: Test better_auth-saml (SAML SSO)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Set up Ruby
        uses: ruby/setup-ruby@95ef2b042f9d7a56d8268cba8559e2842e2ad01b # v1.321.0
        with:
          ruby-version: .ruby-version
          bundler-cache: true
          working-directory: packages/better_auth-saml


      - name: Run tests
        working-directory: packages/better_auth-saml
        env:
          RUBYOPT: -r${{ github.workspace }}/test/support/strict_minitest
        run: bundle exec rake test

  test-sso:
    name: Test better_auth-sso (SSO Plugin)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Fetch upstream Better Auth sources
        run: ./scripts/fetch-upstream-better-auth.sh

      - name: Set up Ruby
        uses: ruby/setup-ruby@95ef2b042f9d7a56d8268cba8559e2842e2ad01b # v1.321.0
        with:
          ruby-version: .ruby-version
          bundler-cache: true
          working-directory: packages/better_auth-sso


      - name: Run tests
        working-directory: packages/better_auth-sso
        env:
          RUBYOPT: -r${{ github.workspace }}/test/support/strict_minitest
        run: bundle exec rake test

  test-stripe:
    name: Test better_auth-stripe (Stripe Plugin)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Fetch upstream Better Auth sources
        run: ./scripts/fetch-upstream-better-auth.sh

      - name: Set up Ruby
        uses: ruby/setup-ruby@95ef2b042f9d7a56d8268cba8559e2842e2ad01b # v1.321.0
        with:
          ruby-version: .ruby-version
          bundler-cache: true
          working-directory: packages/better_auth-stripe


      - name: Run tests
        working-directory: packages/better_auth-stripe
        env:
          RUBYOPT: -r${{ github.workspace }}/test/support/strict_minitest
        run: bundle exec rake test

  test-telemetry:
    name: Test better_auth-telemetry
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Set up Ruby
        uses: ruby/setup-ruby@95ef2b042f9d7a56d8268cba8559e2842e2ad01b # v1.321.0
        with:
          ruby-version: .ruby-version
          bundler-cache: true
          working-directory: packages/better_auth-telemetry


      - name: Run tests
        working-directory: packages/better_auth-telemetry
        env:
          RUBYOPT: -r${{ github.workspace }}/test/support/strict_minitest
        run: bundle exec rake test

  docs-site:
    name: Docs site
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: docs-site
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Fetch upstream Better Auth sources
        working-directory: .
        run: ./scripts/fetch-upstream-better-auth.sh
      - uses: pnpm/action-setup@0977fd99725f1db4007ccb2928dbb4e90d06cc86 # v6.0.10
        with:
          version: 10
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: docs-site/pnpm-lock.yaml
      - run: pnpm install --frozen-lockfile
      - run: pnpm docs:parity-check
      - run: pnpm format:check
      - run: pnpm lint
      - run: NEXT_PUBLIC_URL=https://example.com pnpm build

  ci:
    name: CI
    runs-on: ubuntu-latest
    needs:
      - lint-workspace
      - test-workspace
      - test-core
      - test-passkey
      - test-cli
      - test-api-key
      - test-rails
      - test-sinatra
      - test-hanami
      - test-grape
      - test-roda
      - test-oauth-provider
      - test-redis-storage
      - test-mongodb
      - test-mongo-adapter
      - test-scim
      - test-oidc
      - test-saml
      - test-sso
      - test-stripe
      - test-telemetry
      - docs-site
    if: always()
    steps:
      - uses: re-actors/alls-green@b5b5b37504aa4183270bd3d855c52a67f212be35 # v1.3.0
        with:
          jobs: ${{ toJSON(needs) }}
