# frozen_string_literal: true

source "https://rubygems.org"

ruby file: ".ruby-version"

# Use local better_auth for development
gem "better_auth", path: "../better_auth"
gem "better_auth-passkey", path: "../better_auth-passkey"

gemspec name: "better_auth-rails"

group :development, :test do
  gem "pg", "~> 1.5"
  gem "mysql2", "~> 0.5"
  gem "standardrb", "~> 1.0"
  gem "rspec", "~> 3.13"
  gem "rake", "~> 13.2"
  gem "simplecov", "~> 0.22", require: false
end
